{
  "name": "@pintle/docs-site",
  "version": "0.0.0",
  "private": true,
  "description": "Public documentation site (placeholder; framework added in its module).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"@pintle/docs-site: no tests yet\"",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@pintle/tsconfig": "workspace:*",
    "@types/node": "20",
    "typescript": "^5.4.5"
  }
}
