{
  "name": "@pintle/worker-import",
  "version": "0.0.0",
  "private": true,
  "description": "BullMQ worker: import/load execution jobs (idempotent).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"@pintle/worker-import: no tests yet\"",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@pintle/tsconfig": "workspace:*",
    "@types/node": "20",
    "typescript": "^5.4.5"
  }
}
