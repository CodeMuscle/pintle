{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "@pintle/tsconfig/node-service.json",
  "comment": "Type-checks prisma/ scripts (seed.ts). Separate from the package build tsconfig (src-only); noEmit since seed.ts runs via tsx.",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "."
  },
  "include": ["**/*.ts"]
}
