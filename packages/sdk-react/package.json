{
  "name": "@pintle/sdk-react",
  "version": "0.0.0",
  "private": true,
  "description": "React bindings/components on top of @pintle/sdk-core.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"@pintle/sdk-react: no tests yet\"",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@pintle/tsconfig": "workspace:*",
    "typescript": "^5.4.5"
  }
}
